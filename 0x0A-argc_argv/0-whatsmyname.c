#include <stdio.h>

/**
 * main - program to display its name
 * @argc: an integer
 * @argv: an array of strings
 *
 * Return: an int
 */
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}

